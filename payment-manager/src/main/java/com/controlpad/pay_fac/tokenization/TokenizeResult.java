package com.controlpad.pay_fac.tokenization;

public enum TokenizeResult {
    CREATED_NEW_TOKEN,
    CREATED_NEW_CUSTOMER,
    UPDATED_EXISTING_TOKEN
    // TODO fails and status codes and messages
}
